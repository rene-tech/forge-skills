interface:
  display_name: "Use OpenFold2"
  short_description: "Use and evaluate Use OpenFold2"
  default_prompt: "Use $use-forge-openfold-openfold2-nim to select, format a request for, run, and interpret Use OpenFold2 on Forge."
