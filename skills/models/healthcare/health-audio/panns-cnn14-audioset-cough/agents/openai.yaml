interface:
  display_name: "Use PANNs CNN14 AudioSet Cough Baseline"
  short_description: "Use and evaluate Use PANNs CNN14 AudioSet Cough Baseline"
  default_prompt: "Use $use-forge-panns-cnn14-audioset-cough to select, format a request for, run, and interpret Use PANNs CNN14 AudioSet Cough Baseline on Forge."
