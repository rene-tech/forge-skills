interface:
  display_name: "Use PubMedBERT Base Embeddings"
  short_description: "Use and evaluate Use PubMedBERT Base Embeddings"
  default_prompt: "Use $use-forge-neuml-pubmedbert-base-embeddings-vllm-cuda13 to select, format a request for, run, and interpret Use PubMedBERT Base Embeddings on Forge."
