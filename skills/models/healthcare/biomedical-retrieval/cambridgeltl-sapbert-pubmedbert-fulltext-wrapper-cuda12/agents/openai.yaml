interface:
  display_name: "Use SapBERT PubMedBERT Entity Embeddings"
  short_description: "Use and evaluate Use SapBERT PubMedBERT Entity Embeddings"
  default_prompt: "Use $use-forge-cambridgeltl-sapbert-pubmedbert-fulltext-wra-6f549b5a to select, format a request for, run, and interpret Use SapBERT PubMedBERT Entity Embeddings on Forge."
