interface:
  display_name: "Use PubMedBERT Base Embeddings"
  short_description: "Use and evaluate Use PubMedBERT Base Embeddings"
  default_prompt: "Use $use-forge-neuml-pubmedbert-base-embeddings-tei-cuda-1-9 to select, format a request for, run, and interpret Use PubMedBERT Base Embeddings on Forge."
