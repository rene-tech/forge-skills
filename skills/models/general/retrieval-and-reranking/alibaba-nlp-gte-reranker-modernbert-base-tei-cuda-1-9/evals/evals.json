{
  "cases": [
    {
      "expectedBehavior": [
        "Loads the exact model skill, not the full catalog.",
        "Validates the live Forge route before inference.",
        "Uses the exact request fields and bounds.",
        "Loads audited research before interpreting model quality or output meaning."
      ],
      "id": "exact-model-run",
      "prompt": "Use Forge model alibaba-nlp-gte-reranker-modernbert-base-tei-cuda-1-9 for this task: English retrieval reranking (cross-encoder reranker for query + candidate document pairs). Show the exact request format and explain the output.",
      "shouldActivate": true
    },
    {
      "expectedBehavior": [
        "Loads the exact research reference and the matching group dossier.",
        "Makes only task- and protocol-scoped comparisons.",
        "States insufficient evidence when public protocols are not comparable."
      ],
      "id": "model-selection",
      "prompt": "Should I use alibaba-nlp-gte-reranker-modernbert-base-tei-cuda-1-9 or another Forge model for English retrieval reranking (cross-encoder reranker for query + candidate document pairs)?",
      "shouldActivate": true
    },
    {
      "expectedBehavior": [
        "Does not invent support or a benchmark.",
        "Explains the exact limitation or evidence gap.",
        "Routes to a better task-group candidate when evidence supports one."
      ],
      "id": "decline-mismatched-task",
      "prompt": "Use alibaba-nlp-gte-reranker-modernbert-base-tei-cuda-1-9 for Using gte-reranker-modernbert-base as a drop-in embedding generator for vector search without verification, even if it is not validated for that task.",
      "shouldActivate": true
    }
  ],
  "modelSlug": "alibaba-nlp-gte-reranker-modernbert-base-tei-cuda-1-9",
  "schemaVersion": "1.0.0",
  "skill": "use-forge-alibaba-nlp-gte-reranker-modernbert-base-tei-cuda-1-9"
}
