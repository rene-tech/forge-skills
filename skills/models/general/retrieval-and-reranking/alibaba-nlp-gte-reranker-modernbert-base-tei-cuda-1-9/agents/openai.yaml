interface:
  display_name: "Use GTE Reranker ModernBERT Base"
  short_description: "Use and evaluate Use GTE Reranker ModernBERT Base"
  default_prompt: "Use $use-forge-alibaba-nlp-gte-reranker-modernbert-base-tei-cuda-1-9 to select, format a request for, run, and interpret Use GTE Reranker ModernBERT Base on Forge."
