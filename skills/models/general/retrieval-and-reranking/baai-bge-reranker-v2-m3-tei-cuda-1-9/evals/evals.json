{
  "cases": [
    {
      "expectedBehavior": [
        "Loads the exact model skill, not the full catalog.",
        "Validates the live Forge route before inference.",
        "Uses the exact request fields and bounds.",
        "Loads audited research before interpreting model quality or output meaning."
      ],
      "id": "exact-model-run",
      "prompt": "Use Forge model baai-bge-reranker-v2-m3-tei-cuda-1-9 for this task: Reranking top-k retrieval candidates (query, passage pairs) in multilingual retrieval and open-domain QA pipelines. Show the exact request format and explain the output.",
      "shouldActivate": true
    },
    {
      "expectedBehavior": [
        "Loads the exact research reference and the matching group dossier.",
        "Makes only task- and protocol-scoped comparisons.",
        "States insufficient evidence when public protocols are not comparable."
      ],
      "id": "model-selection",
      "prompt": "Should I use baai-bge-reranker-v2-m3-tei-cuda-1-9 or another Forge model for Reranking top-k retrieval candidates (query, passage pairs) in multilingual retrieval and open-domain QA pipelines?",
      "shouldActivate": true
    },
    {
      "expectedBehavior": [
        "Does not invent support or a benchmark.",
        "Explains the exact limitation or evidence gap.",
        "Routes to a better task-group candidate when evidence supports one."
      ],
      "id": "decline-mismatched-task",
      "prompt": "Use baai-bge-reranker-v2-m3-tei-cuda-1-9 for Using this reranker checkpoint as an embedding model (embedding-model substitute), even if it is not validated for that task.",
      "shouldActivate": true
    }
  ],
  "modelSlug": "baai-bge-reranker-v2-m3-tei-cuda-1-9",
  "schemaVersion": "1.0.0",
  "skill": "use-forge-baai-bge-reranker-v2-m3-tei-cuda-1-9"
}
