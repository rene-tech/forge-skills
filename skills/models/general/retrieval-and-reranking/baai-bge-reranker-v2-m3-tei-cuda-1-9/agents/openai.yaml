interface:
  display_name: "Use BGE Reranker v2 M3"
  short_description: "Use and evaluate Use BGE Reranker v2 M3"
  default_prompt: "Use $use-forge-baai-bge-reranker-v2-m3-tei-cuda-1-9 to select, format a request for, run, and interpret Use BGE Reranker v2 M3 on Forge."
