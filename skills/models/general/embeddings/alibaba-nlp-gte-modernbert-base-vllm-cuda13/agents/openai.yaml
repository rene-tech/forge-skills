interface:
  display_name: "Use GTE ModernBERT Base"
  short_description: "Use and evaluate Use GTE ModernBERT Base"
  default_prompt: "Use $use-forge-alibaba-nlp-gte-modernbert-base-vllm-cuda13 to select, format a request for, run, and interpret Use GTE ModernBERT Base on Forge."
