interface:
  display_name: "Use MOVA 720p"
  short_description: "Use and evaluate Use MOVA 720p"
  default_prompt: "Use $use-forge-openmoss-team-mova-720p-sglang to select, format a request for, run, and interpret Use MOVA 720p on Forge."
