interface:
  display_name: "Use MOVA 360p"
  short_description: "Use and evaluate Use MOVA 360p"
  default_prompt: "Use $use-forge-openmoss-team-mova-360p-sglang to select, format a request for, run, and interpret Use MOVA 360p on Forge."
