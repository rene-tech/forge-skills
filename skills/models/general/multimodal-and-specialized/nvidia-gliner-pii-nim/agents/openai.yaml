interface:
  display_name: "Use NVIDIA GLiNER PII"
  short_description: "Use and evaluate Use NVIDIA GLiNER PII"
  default_prompt: "Use $use-forge-nvidia-gliner-pii-nim to select, format a request for, run, and interpret Use NVIDIA GLiNER PII on Forge."
