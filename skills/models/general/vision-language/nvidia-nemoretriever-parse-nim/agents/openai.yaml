interface:
  display_name: "Use NVIDIA NeMo Retriever Parse"
  short_description: "Use and evaluate Use NVIDIA NeMo Retriever Parse"
  default_prompt: "Use $use-forge-nvidia-nemoretriever-parse-nim to select, format a request for, run, and interpret Use NVIDIA NeMo Retriever Parse on Forge."
