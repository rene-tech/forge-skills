{
  "cases": [
    {
      "expectedBehavior": [
        "Loads the exact model skill, not the full catalog.",
        "Validates the live Forge route before inference.",
        "Uses the exact request fields and bounds.",
        "Loads audited research before interpreting model quality or output meaning."
      ],
      "id": "exact-model-run",
      "prompt": "Use Forge model nvidia-nemoretriever-parse-nim for this task: Document OCR and structured extraction from document images, producing reading-order text, per-element class labels, and bounding boxes. Show the exact request format and explain the output.",
      "shouldActivate": true
    },
    {
      "expectedBehavior": [
        "Loads the exact research reference and the matching group dossier.",
        "Makes only task- and protocol-scoped comparisons.",
        "States insufficient evidence when public protocols are not comparable."
      ],
      "id": "model-selection",
      "prompt": "Should I use nvidia-nemoretriever-parse-nim or another Forge model for Document OCR and structured extraction from document images, producing reading-order text, per-element class labels, and bounding boxes?",
      "shouldActivate": true
    },
    {
      "expectedBehavior": [
        "Does not invent support or a benchmark.",
        "Explains the exact limitation or evidence gap.",
        "Routes to a better task-group candidate when evidence supports one."
      ],
      "id": "decline-mismatched-task",
      "prompt": "Use nvidia-nemoretriever-parse-nim for Tasks requiring guaranteed production-grade reliability, regulatory certification, or formal external validation without additional engineering controls, even if it is not validated for that task.",
      "shouldActivate": true
    }
  ],
  "modelSlug": "nvidia-nemoretriever-parse-nim",
  "schemaVersion": "1.0.0",
  "skill": "use-forge-nvidia-nemoretriever-parse-nim"
}
