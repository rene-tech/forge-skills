interface:
  display_name: "Use NVIDIA Cosmos Policy LIBERO 2B (FP8, Blackwell/Hopper)"
  short_description: "Use and evaluate Forge model nvidia-cosmos-policy-libero-predict"
  default_prompt: "Use $use-forge-nvidia-cosmos-policy-libero-predict2-fp8 to select, format a request for, run, and interpret Use NVIDIA Cosmos Policy LIBERO 2B (FP8, Blackwell/Hopper) on Forge."
